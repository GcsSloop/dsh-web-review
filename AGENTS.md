# AGENTS.md — dsh-web-review (webview panel + element annotation)

Rules for the dsh-web-review plugin: the webui's right-side page-preview panel, in-iframe element selection and commenting, and the AI collaboration loop that turns annotations into workspace source edits.

**This plugin lives OUTSIDE the harness checkout** — it is developed in the user's own repo (dsh-web-review) and loaded into a `dsh web` instance by a launch overlay, with zero modifications to the deepseek-harness source tree. Resolve the active harness root with `scripts/harness-path.ts` (or `DSH_HARNESS`), then read `<harness>/packages/client/AGENTS.md`, `<harness>/.agents/notes/implemented/architecture/2026-07-22-slot-type-chain-implementation.md`, and `<harness>/AGENTS.md` before touching slots, stores, or inject faces. Relative links to an assumed sibling checkout are forbidden because they resolve incorrectly from Codex worktrees.

## Harness compatibility baseline

- The reviewed runtime baseline is the alpha.5 release line: the `dsh-v0.1.2-alpha.5` tag (commit `db6bdc3576` on `deepseek-harness` master, npm artifacts published 2026-09-02). A harness update is a contract migration, not a blind rebuild: compare the upstream client rules, manifest scanner, Web profile CLI, agent events, and Context renderer before declaring compatibility. Development/acceptance/E2E run the alpha.5 built CLI from `~/Program/deepseek-harness-alpha5` (`DSH_HARNESS`); the rc.8 checkout at `~/Program/deepseek-harness` stays for the rc.8 line.
- Earlier corridor (rc.6→rc.8), verified against the rc.8 npm surfaces and source checkout: the client module table dropped `dsh-client-web-react` and `dsh-client-schema-form` (slot rendering now ships from `dsh-client-ui-renderer`); the client module system moved from the `window.__DSH_MODULES__` page global to the `ctx.modules` service; `dsh-client-ui-commands` submissions gained an image-acceptance envelope policy; and the rc.8 Context renderer removed the `conversation.chat.contextview` chain slot, so the browser-comments plugin context now declares the standard `snapshot` form (`source.form` + `sections`, typed by `@deepseek-ai/dsh-llm/message`) rendered by the harness's ContextInjectionRow. Our plugin's pinned surface (slots, store seat, commandUi, locale, layout) typechecks and gates green on rc.8 unchanged.
- The client manifest shape is nested `dsh.client`; `dshClient` is legacy and ignored. Cross-package slot contributions use `ctx.slots.inject(slotName, callback)`. Annotation injection uses the `agent/pre-step` waterfall and appends its sourced user message only to an `enter` decision; `reject` is preserved unchanged. Host routes register through `ctx.webServer`; Skills use `SkillRegistry`; the client command surface is `ctx.commandUi` from `@deepseek-ai/dsh-client-ui-commands`.
- The public npm `0.1.2-alpha.5` artifacts expose the alpha.5 service names and declarations. Use `@deepseek-ai/dsh-client-ui-commands`, `ctx.commandUi`, `ctx.webServer`, and the published `SkillRegistry` declarations directly; do not restore the obsolete singular command package or the 0811 service names.
- The Web profile accepts external launch patches through `--patch`. Do not restore `--config`, direct execution of `apps/cli/src/bin.ts`, `agent/prompt-submit`, `PromptDecision`, or `additionalContexts` from older snapshots.
- Development, acceptance, and E2E launch the explicit `DSH_HARNESS` checkout's built `apps/cli/lib/bin.js` through Node, so the CLI uses the harness's own dependency graph. The source package is resolved through the profile-local source-package link described below. Ordinary install, typecheck, build, unit/component tests, packaging, and npm release use pinned public `@deepseek-ai/*` packages and must not require a Harness checkout or npm read credential.

## Loading model (why development links the source package into the profile)

- `dsh web` boots the `web` profile's ordered bundle layers and applies each `--patch` file after the profile/user layers. The Loader and client-modules resolve package names from the profile composition anchor, which does not contain this external checkout.
- Harness alpha.5's built CLI uses native ESM. Its Loader normalizes an absolute path to a `file:` URL, and Node rejects directory imports; the old absolute-directory plus root `index.ts` channel is invalid. A file path would load the node half but still break client-modules' independent `<name>/package.json` scan.
- Development links the checkout under its real package name `@canglongcl/dsh-web-review` (alpha.5 requires the loader row name to equal the package.json name — DSH-0.1.2-A1-26). Before Web boot, `scripts/profile-plugin-link.ts` creates or repairs exactly one symlink at `$DSH_HOME/profiles/web/node_modules/@canglongcl/dsh-web-review` pointing to this checkout's package directory. Loader import and client-modules manifest scanning then use ordinary package resolution from the profile anchor. A real (non-symlink) package at that name — an installed official bundle — is never replaced: remove it first or use an isolated `DSH_HOME`.
- The launch overlay (`cordis.yml`) inserts one row: `- insert: - { id: dsh-web-review, name: '@canglongcl/dsh-web-review' }`. The **tsdown client-bundle banner id must equal the entry name** (`__ModuleLoader__.load({ id: ... })` is checked against the boot-graph row id by `arrive()`); `scripts/development-entry.ts` is the source shared by generated `entry-name.json`, `cordis.yml`, and the profile-link materializer.
- **Official profile-bundle publication:** the source-checkout channel and the official bundle carry the same package-name banner id (at alpha.5 the boot graph row IS the package name). `pnpm package:official` stages `lib/client-official.js` plus its map in a prebuilt tarball whose manifest declares `dsh.bundle.patch`; the dev-channel copy stays out of the official allowlist.
- The generated `dist/package/` allowlist is exact: package manifest, `cordis.patch.yml`, self-contained `lib/index.js`, `lib/client-official.js` plus its map, the isolated-frame `lib/bridge.js` plus its map, the Chinese and English root READMEs, their two screenshot assets, and their GIF demo. Source, `.git`, `node_modules`, the profile-local development link/configuration, and the development-channel client bundle must never enter the official package.
- The built node half must stay **self-contained**: the Loader imports the external package from its own directory, which must not require a local `node_modules`. Small DSH construction helpers (`createUserMessage`, `SessionId`) may be value-imported in source only when tsdown inlines them into `lib/index.js`; a surviving bare runtime import is a load-order regression.
- HMR: the repo's `pnpm run dev:web` watch scans only harness `packages/*/*`; the external bundle is watched by the package's OWN tsdown watch (`pnpm run build:watch`). The alpha.5 Web profile mounts its HMR receiver unconditionally and broadcasts rebuilt frames regardless of who rewrote the served bundle; there is no `--dev` Web flag.

## Repository conventions

- **The whole repo is TypeScript** — scripts/, demo/, and tests/ included. `scripts/*.ts` and `demo/server.ts` run via tsx (`pnpm dev`, `pnpm demo`, `pnpm gen-config`); they are typechecked by the `tsconfig.scripts.json` project in the solution.
- **Generated files are gitignored**: `cordis.yml` and `packages/dsh-web-review/entry-name.json` are produced by `pnpm gen-config` from the stable source package name. `dist/` is rebuilt by `pnpm package:official`. `lib/`, `dist/`, `*.tsbuildinfo`, `.env*`, `*.log`, `coverage/`, `test-results/`, `playwright-report/`, `.artifacts/` are also ignored. A commit must not add them; `pnpm check` fails if gen-config would change the files or if the official package allowlist drifts. The committed `docs/assets/*.jpg` + `web-review-demo.gif` are an exception by design: they are generated by `pnpm media` (operational runbook in the `readme-media` skill, `.agents/skills/readme-media/SKILL.md`) and shipped in the package — regenerate them instead of hand-editing.
- **Pre-commit hook** (zero-dependency, `core.hooksPath` → `scripts/git-hooks`, installed by the `prepare` script on `pnpm install`): runs the fast gate — `tsc -b --force`, `vitest run`, and gen-config idempotence. The e2e browser suite is deliberately NOT in the commit gate (it spawns services and needs a provider key); run `pnpm test:e2e` for UI changes.
- **Quality gate**: `pnpm check` (all gates) / `pnpm check --e2e` (plus the browser suite). Gate list: typecheck, unit suite, gen-config idempotence, current `dsh.client` manifest shape, both channel banner-id contracts, native-ESM alias loading, self-contained node/bridge artifacts, tsdown build, official `dsh.bundle` manifest, and exact package staging allowlist. Pre-commit runs the fast subset.
- **npm release workflow:** `.github/workflows/release-npm.yml` anonymously installs the pinned public npm development packages, runs the npm-only gate, uploads the exact checked tarball, and publishes that artifact publicly for a matching `v*` tag from `CanglongCl/dsh-web-review`. The publish job uses npm Trusted Publishing with GitHub OIDC (`id-token: write`) and must not receive a long-lived npm write token. Run the explicit-Harness E2E gate separately before tagging a release.
- **Changelog and GitHub releases:** `pnpm changelog` (`scripts/changelog.ts`) regenerates `CHANGELOG.md` and per-version release notes from git tags and Conventional Commits (Keep a Changelog format; feat→Added, fix→Fixed, perf/refactor/revert/ui-webview→Changed, all other types→Internal, release/merge commits skipped, breaking commits in a dedicated subsection). `pnpm release:beta` regenerates the changelog before its `release: bump` commit; publishing-mode `release:verify` requires `CHANGELOG.md` to lead with the tagged version. After npm publish, the workflow creates the matching GitHub release with `gh release create` (prerelease flag for prereleases, notes from `pnpm changelog --version`, tarball + SHA256SUMS attached) and must never use a third-party release action.
- **Beta channel:** a `vX.Y.Z-beta.N` tag publishes the identical checked tarball to the `beta` dist-tag (install with `@canglongcl/dsh-web-review@beta`); other prereleases keep `next`, stable keeps `latest`. `pnpm release:beta [base-version]` bumps both manifests to the next beta version, runs `release:verify`, commits `release: bump <version>`, creates the annotated `v<version>` tag, and pushes (`--dry-run` prints the plan only). The publish job's dist-tag mapping and the script's existence are pinned by `verify-release.ts`.
- The repo is a git repository; commit messages follow the convention of the first commit.

## npm publication boundary

- The npm package identity remains `@canglongcl/dsh-web-review`, and every publishable manifest plus publish command keeps `access: public`.
- The source manifest keeps `private: true` to prevent publishing the workspace package directly. The tightly allowlisted staging manifest omits that field and declares `publishConfig.access: public`.
- Never print, commit, embed, forward, or persist real npm/GitHub/provider credentials in repository files, workflow arguments, logs, screenshots, artifacts, or model-facing context.

## Scope and ownership

- **One dual-face package plus one isolated-frame artifact.** The node half (`src/index.ts`) hosts the same-origin `/webview-preview-sessions` control route, the independent loopback Preview server, the `/webview-annotations` pending-snapshot route, the stable Preview capability system-prompt section, and one `agent/pre-step` listener. The DSH browser half (`src/client/`) owns the tab, host editor, assistant-link delegation, store, and acknowledged dock sync. `src/bridge/` builds to a separate IIFE that runs only inside the random Preview Origin. The node half is a capability-scoped page transport, not an unauthenticated fetch API.
- **Plugin export shape:** named exports `name` / `inject` / `Config` (when present) / `apply`, no default export — a default export makes the Loader discard the namespace (upstream postmortem 0001).
- **The preview is a right-Sidebar page tab** and the annotation strip is a **`conversation.input.dock` entry** (`id: 'webview-annotations'`, order 15, GoalBar style). Both register the SAME apply-constructed store handle (`store: webviewStore` from `createWebviewStore()`, the ui-conversation chatStore multi-registration pattern: a `StoreHandle` in the `store` seat is shared per scope, a `StoreFactory` would instantiate per entry). `PreviewTabBody` (`src/client/sidebar/PreviewTab.tsx`) owns the transport ladder, the bridge, the picker, the URL row, and the host editor; the dock owns the capsule, the acknowledged sync, and the assistant-link delegation. There is no `conversation.view` contribution any more: the preview never replaces the chat area. The harness shell (ui-layout) is NOT modified, and the right-Sidebar host package is reached only through `ctx.get('sidebarRightTabs')` / `ctx.get('sidebarRight')` — never imported, so its absence leaves the dock working. Both roots carry `data-webview-ui`; the tab root also carries `data-webview-sidebar-preview`.
- **The sidebar editor is a bottom sheet, not a floating card.** A native shell panel is a separate view that no host DOM can paint across, and the column is narrow, so the pane splits into a page area and a sheet (`AnnotationEditor`'s `docked` prop): the placeholder reports the smaller rectangle, which is what shrinks the shell's panel, and the sheet's height is the user-draggable geometry (top edge only). Floating placement remains the default for any host that renders the editor inside the page area.
- **A tab body is unmounted whenever another sidebar tab becomes active**, so `PreviewTabBody` caches its session per tab id in a module-level map and a remount re-attaches (re-reporting the panel's rectangle and re-probing the bridge) instead of opening a new session. Releasing on unmount would reload the page on every tab switch.
- Cross-plugin panel control goes through the plugin's own store and the `ctx.layout` service only where already offered (`closeDetails`); nothing reaches into another package's store or registers into a slot it did not declare.
- **Product copy is Chinese; code comments, JSDoc, and this file are English** (upstream convention). Locale files contain UI copy only. The locale-independent model context template is pinned in node-owned `annotation-context.ts`, never assembled by components.

## Isolated Preview transport

- **Control plane:** the DSH host exposes exact `POST | DELETE /webview-preview-sessions`. It requires same-origin browser `Origin`, JSON, and the plugin client header; it has no CORS grant. `POST { target }` accepts any credential-free absolute HTTP(S) URL and returns a strict `{ sessionId, frameUrl, frameOrigin, targetOrigin, channel }`. `targetOrigin` must match the entry target encoded in `frameUrl` and bounds accepted ready-page URLs. `DELETE { sessionIds }` revokes capabilities. No target bytes are served from the DSH host Origin, and the legacy `/webview-proxy` host route must not return.
- **Origin boundary:** the node face starts a separate server bound to `127.0.0.1` on an ephemeral port. Every top-level target gets a random 128-bit `<session>.localhost:<port>` Origin, distinct from DSH and every other preview. Sessions expire, are capped, and are revoked on frame disposal. The iframe is sandboxed but keeps `allow-scripts allow-same-origin` because the random Origin itself is the isolation boundary.
- **Target binding and rebinding defense:** a session accepts requests only for its normalized target Origin. The first DNS resolution is pinned for the session; native HTTP(S) requests retain the original Host/SNI while connecting only to those addresses. Same-Origin redirects stay in the session. Cross-Origin markup navigation or redirects create a new random session through a bounded server-authored handoff document. Never replace this with an open redirect/fetch endpoint or reuse a Preview Origin across target Origins.
- **Transport limits:** only GET/HEAD/POST are forwarded; request/response bodies are capped at 10MB, upstream work times out around 15s, redirect count and cross-Origin handoff depth/fanout are bounded. The DSH host Origin's own cookies/authorization are never forwarded, because the frame Origin is a different host; `previewCookies` (default on) instead keeps one target-Origin cookie jar (`src/preview-cookies.ts`) — upstream `set-cookie` values are stored per bound target Origin and replayed only there, the frame browser's own cookies are forwarded, and `relayedPreviewCookie` drops `Domain`/`Secure` that the loopback `http` frame cannot honour before the value reaches the browser. Framing/CSP, connection, reporting, and host-mutating headers are stripped; HTML receives the plugin-owned exact-parent `frame-ancestors` policy. The handler cancels streams as soon as a cap is crossed.
- **HTML rewriting:** use `parse5`; no regex-over-HTML. Remove CSP/refresh meta directives and inject, before page-authored scripts, the target-path `<base>`, the address-normalizing `history.replaceState` bootstrap, the frozen bridge config, and `bridge.js`. The frame address is normalized to the target's own path/query so `location` and framework routers match the original page; root-relative and plain-relative URL-bearing attributes resolve natively against the isolated Origin, absolute same-target values are moved onto the frame Origin, cross-Origin static `href`/`action` values use the handoff route, while cross-Origin POST forms are rejected. Cross-Origin subresources retain browser-native CORS, root-relative runtime requests map to the bound target Origin, and same-Origin POST forms forward exact bytes.
- **Native panel transport:** the shell hosts the page in its own WKWebView, so the plugin's loopback endpoint is unreachable from an **HTTPS** document: WebKit refuses the insecure request (`fetch` rejects with "Load failed", `sendBeacon` returns `false`, and `localhost` behaves the same), which left the panel rendered but never handshaken. The shell therefore installs a `WKScriptMessageHandler` named `dshWebReview` on the panel webview (`src-tauri/src/panel_bridge.rs`) and relays each payload from its own process, where browser transport rules do not apply; `/panel/open` carries the relay endpoint so the shell knows where to post. The bootstrap publishes the handler name in `__DSH_WEB_REVIEW_NATIVE_IPC__`, `src/bridge/index.ts` prefers it and holds messages briefly (`queueNative`) while the handler is being installed, and the loopback endpoint stays the fallback an HTTP page uses. The shell's `/panel/state` reports `relay: true` when a panel has one.
- **A panel with no client is hidden, then closed.** Reloading the DSH page destroys the client half while the node half keeps running, which used to leave the shell's panel drawn over the interface with nothing able to close it. `NativeBrowserSessions.watchDetach` hides the panel after a 4s grace period with no SSE listener and releases the session after 60s; a sidebar tab switch re-attaches well inside the grace period, so it survives untouched.
- **External links leave through the shell.** A click on the address row's external action is relayed by `/webview-open-external` to the shell's `/shell/open`, which launches the user's default browser (`open`); a host without a shell answers `{ ok: false }` and the client keeps its ordinary `target="_blank"` behavior. The action only takes that route when the shell itself is hosting the preview (`descriptor.mode === 'native'`), so a browser deployment is untouched.
- **Real-browser transport (CDP):** `src/cdp-transport.ts` is a dependency-free CDP client (launch/attach, flat sessions, screencast, evaluate, screenshot, init scripts, bindings, viewport, input) and `src/browser-preview.ts` owns one Chromium page per session. The control route accepts `{ target, mode }`; `/webview-browser-stream` holds one SSE frame/state stream and `/webview-browser-input` carries input, viewport, commands, and the down-channel bridge message. Each session installs `Runtime.addBinding('__dshWebReviewSend')` and injects the exact-Origin bridge bootstrap before its first navigation, so `src/bridge/index.ts` delivers through the binding when it exists and keeps `parent.postMessage` for a hosted iframe; host commands arrive through `window.__dshWebReviewReceive`. The client renders `src/client/browser-surface.ts` (canvas + input + carrier) instead of an iframe, and `src/client/sidebar/PreviewTab.tsx` registers the optional right-Sidebar page tab through the host's `sidebarRightTabs`/`sidebar.right.pane.tab` seats, probed at apply and never injected.
- **Build boundary:** `src/bridge/index.ts` has its own TypeScript project and tsdown IIFE output. Both source-development and official packages use the same generic bridge artifact; unlike the client bundles, it contains no package-loader banner id.

## Picker and bridge contract

- **No direct frame access:** production host code must never read `iframe.contentDocument`, call page functions, retain `Element` references, or infer safety from a successful cross-frame read. The parent talks only to the exact `iframe.contentWindow` at the descriptor's exact Origin, target Origin, protocol version, and random channel. Page-reported URL Origins must equal the server-bound `targetOrigin`; handoffs are accepted only from the current expected frame identity.
- **Bridge ownership:** `src/bridge/index.ts` captures the native parent messaging primitive before page scripts, deletes its bootstrap global, installs picker style/behavior, and owns all live DOM handles and rollback ledgers. Host commands are an explicit discriminated union; frame responses/events are strictly decoded and bounded in `preview-contract.ts`. The channel routes one session but is not a claim that page evidence is truthful: page URL/title/DOM metadata remain untrusted.
- **Serializable editor protocol:** the host receives bounded snapshots, geometry, navigation availability, and serialized tree nodes. It sends opaque handles for select/navigate/style/text/commit/cancel operations. Comments never enter the frame. The solid editor remains a host React overlay, so page opacity, transforms, fonts, and stacking contexts cannot affect it.
- **Picker behavior:** hover highlights the deepest element under the pointer; in pick mode a click on an unannotated element hands it to `onPick` (the parent opens the host editor); a click on an already-annotated element or its marker hands the pick id to `onMarkClick` (re-open that editor). Clicks never navigate (`preventDefault`). The numbered marker circles are the only injected chrome and reposition on scroll/resize. Esc with no editor transaction or a second toggle deactivates pick mode.
- **cssPath:** delegated to `css-selector-generator` (inlined into the bridge artifact) with the priority `['id', 'class', 'tag', 'nthoftype']` — order is priority, output is the shortest unique selector, so index segments appear only when uniqueness requires them; `null` falls back to the bare tag.
- **Snapshot fields** (exact keys, mapped to the wire shape by `annotation-snapshot.ts`): `tagName`, `id`, `className`, `cssPath` (shortest unique selector — runtime anchor for marker rebuilds), `fullPath` (complete ancestor chain), `label` (accessible label), `role` (explicit/implicit ARIA role), `stableClasses` (semantic classes; utility/variant tokens and fully opaque hashes filtered, but `<hash>_<name>` CSS-module classes are kept for their searchable semantic suffix), `inToolChrome` (true when the element sits inside this plugin's own `[data-webview-ui]` chrome, e.g. dogfooding a DSH Web GUI), `anchor` (framework source anchor or null), `outerHTML` (≤1500 chars), `textContent` (≤300 chars, crossing the node wire for unlabeled targets), `rect`, computed `display/position/font-size/color/background-color/margin/padding/width/height`. Caps are contract — enforce at capture and at the node wire boundary, and test exact limits. The source anchor comes from `source-anchor.ts`: framework-agnostic probing of React fiber `_debugSource`, Vue `__file`, and Svelte `__svelte_meta` (defensive reads, null in production builds/unknown frameworks).
- **The page always loads through a random Preview Origin** — there is no direct remote iframe fallback. Element picking is available only after the exact-Origin bridge handshake; a login performed inside the frame works through the target-Origin cookie jar, while original-Origin-enforcing or anti-bot pages may still fail to render (see Known limitations).

## Annotation message and AI collaboration

- **Structured wire, node-owned text.** The browser POSTs exact-shape `{ sessionId, page, comments[] }`, never XML/Markdown. Each comment carries bounded `changes[]`, nullable `textChange`, and edit-time `viewport`; only changed values cross the boundary and every non-empty comment must contain user intent. User-authored values fail visibly instead of being silently truncated. `annotation-contract.ts` pins request/count/field/context caps; `annotation-context.ts` strictly validates object keys, the credential-free HTTP(S) page policy, the shared property allowlist, and is the single assembly point. Malformed, oversized, duplicate, wrong-content-type, and unknown-session requests fail visibly.
- **Stable Browser Comments format.** The context is English regardless of UI locale and starts with `# Browser comments`. Each full snapshot says it supersedes earlier snapshots. Page/target/original-value metadata is explicitly untrusted page evidence; comments, requested values, and text replacements are user-authored input. The target block carries `Target owner:` when `inToolChrome` is true (the element belongs to this plugin's own nested chrome, so the edit lands in this plugin's source, not the previewed page) and `Target text:` (bounded `textContent`) when the element has no accessible label. The optional `Browser annotation` block includes viewport plus `property: before -> after` lines. Raw outerHTML, full computed styles, rectangles, editor state, and screenshots are excluded.
- **Send-time context path.** The node face resolves the live branded `SessionId` and stores one deduplicated pending full snapshot. Its `agent/pre-step` listener delegates with `next()`, preserves `reject`, and on `enter` appends one user-role message with `{ kind: 'plugin', plugin: 'dsh-web-review' }` after the returned messages; it never rewrites claimed message content. The append is bound to the user's prompt: it rides only a step whose claimed batch carries a `source.kind === 'user'` message, so in queue mode the snapshot stays pending behind the busy agent's intermediate tool steps and enters the LLM together with the queued message instead of being injected early and consumed before that message. The exact plugin `user/message` session event consumes the matching pending snapshot, so a rejected/aborted admission cannot lose it and a newer concurrent snapshot cannot be cleared by an older event. Clearing before send removes pending state and injects nothing; `agent/disposed` releases state.
- **Stock input machine remains authoritative for drafts.** Annotation mode adds a dedicated Send button, enabled only after a non-empty snapshot is acknowledged with a node-issued `snapshotId`. With a non-blank composer draft it calls the public `inputActions.submit()` action, so ordinary serialization, queueing, failure handling, and successful draft clearing remain stock behavior; the plugin never reads or sends a second copy of that text. Only the durable plugin Context record carrying that exact id clears the dock; generic/older human records cannot acknowledge a newer snapshot. The stock machine deliberately rejects an empty draft, so the no-draft arm calls the scoped public `conversation.send` with the localized fixed request (“请根据页面批注修改前端实现。”). Failures keep annotations visible and retryable.
- **No model-facing tool is added:** the context is a logged user-role plugin message; the model acts with existing workspace tools (`tool-fs`, bash). Adding a tool requires a consumer that proves the message form insufficient.
- The AI's workspace is the session's workspace root — the dev instance is launched with `cwd` = the user's project directory (or the GUI workspace picker), so `tool-fs` edits land in the project the iframe previews.

## UI discipline

- Follow the four props shares, the store-factory rule, and the data-access ladder (upstream client rules): business state (url, picks, comments, pick mode, panel open/width/split) lives in `createWebviewStore()` declared at register. Components receive everything through props — no ctx, no React contexts, no hand-made hooks.
- **Assistant-link delegation:** the always-mounted annotation dock owns one document capture-phase listener, including while it renders no capsule. It intercepts only unmodified left-clicks on credential-free absolute HTTP(S) anchors inside Harness alpha.5's `[data-chat-flow-kind="assistant-step"]`; user/tool/modifier/middle clicks and `[data-webview-ui]` chrome keep their normal behavior. The injected dock callback normalizes the URL, writes it through the plugin store actions, clears stale picks, closes Details through `ctx.layout`, and opens the right-Sidebar preview tab at that address through `ctx.sidebarRight.openTab`.
- **Model guidance:** the node face contributes the reviewed, locale-independent `plugin:dsh-web-review-preview` system section at order `-97`. It states the capability and review loop: emit an absolute HTTP(S) Markdown link for a page worth reviewing (and verify a local development server when source edits are expected), then let the user annotate it in Preview.
- The preview has two chrome states. Browse mode is the URL row: `back · forward · reload · address field (with the inline external-open action) · annotate`. Annotation mode replaces the row with the light-blue toolbar: exit, clear, centered current URL, and the counted Send button. The selected element gets a solid-white host editor: collapsed comment pill first, a Select-triggered serialized DOM hierarchy tree, and an Adjust-triggered Figma-style inspector for the shared visual-property allowlist and safe direct-text editing. Select and Adjust are mutually exclusive. The selector moves to the first child, parent, previous sibling, or next sibling from one compact four-button row labeled 子级/父级/上一个/下一个 (Child/Parent/Previous/Next); each button carries a small trailing shortcut keycap instead of a redundant direction icon. The normal pick flow focuses the host-owned canvas surface, where Enter, Backslash, Shift+Tab, and Tab issue the same bridge movements without exposing those host events to page listeners. Whenever the tree is closed, a compact current-target bar remains below the comment input; each successful shortcut replaces its target text with a short directional transition, boundaries leave it unchanged, and reduced-motion removes directional movement. Host toolbar buttons and editor inputs keep native behavior. The bridge owns a page-side capture fallback when the iframe is explicitly refocused, while editable page controls retain native keys. Switching targets restores the previous element's transaction preview, preserves the comment draft, and never carries its style/text diffs to the new target. The editor prefers a clear region above/below the selected target, shrinks its scrollable body before overlap, then tries a clear horizontal side on wide frames; overlap is allowed only when a large target leaves no usable region. A docked host skips that placement entirely: the sheet's height is the transaction's geometry, resized from its top edge, and the previous height is what the next element inherits. Changed rows expose individual reset actions. The composer dock renders ONE compact capsule (count + sync state + clear), not one chip per pick. Hover or focus opens a light detail card above it; row click re-opens the same host editor.
- Live element references and rollback ledgers live only in bridge-private maps/weak maps, never in React, the store, or message payloads. Before the first temporary preview write, record the exact inline value and priority; text edits are limited to one direct text node. Reset, Cancel, remove, clear, successful send, navigation, and unmount must restore the exact original DOM state. Remaining picks re-anchor by cssPath and replay committed preview values after frame reload.

## Known limitations (do not "fix" into breakage)

- **Preview Origin isolates capabilities, not truth.** Arbitrary HTTP(S) page scripts execute on a random Origin and cannot obtain DSH same-origin access. They can still influence their own DOM and therefore all page URL/title/selector/anchor/snapshot fields remain untrusted evidence. Only bounded user-entered comments and requested values are treated as user intent.
- **Annotation preparation and stock-composer Send are not atomic.** The public browser input service has no general pre-submit transaction. The capsule exposes preparing/ready/error; users must wait for “发送时注入”. The pre-step waterfall appends one separately sourced message only after delegating to `next()` and receiving `enter`; claimed-message rewriting and delay-based race hiding remain forbidden.
- **Absolute URLs hardcoded in page JS** (for example `fetch('https://origin/api')`) and WebSocket endpoints are not rewritten. Plain-relative calls resolve through `<base>` and root-relative HTTP requests map to the session's bound target Origin. Do not rewrite JavaScript source to disguise this limitation.
- **Dev-server HMR websockets** do not survive the proxy; the page renders but live-reload is absent.
- **Server-side fetch carries only target-Origin cookies** — a page signed in inside Preview keeps working through the cookie jar, but sites that enforce their original browser Origin, require client certificates, or run anti-bot challenges may still not render. There is no unsafe direct-mode fallback.
- **Programmatic cross-Origin location assignment can leave the bridge.** Rewritten static markup navigation and server redirects use handoffs, but dynamically created links or arbitrary page JavaScript can still replace the iframe location directly. The host must fail closed (picker unavailable), never fall back to direct DOM access.
- **The source-package link is profile-local state**: moving or switching the repo is handled by the next `pnpm dev`, E2E, or acceptance launch repairing its exact symlink. If a real file or directory occupies the reserved package name (an installed official bundle), launch fails and asks for explicit removal instead of replacing it.
- These are documented for users in the package README (Known Limitations section) and in panel hints; changing them needs a consumer-driven design.

## Testing

- **Pure-function suites** for `rewrite.ts` (attribute forms, base injection, non-HTML pass-through, exact caps) and picker helpers (cssPath, snapshot truncation) — node env for the node half; `// @vitest-environment jsdom` pragma for browser halves.
- **One REAL-composition test for the node half** (upstream rule, docs/testing.md): boot a test-only `cordis.yml` through the Loader with the webserver and this package, create a session through the real control route, and assert that its distinct Preview Origin serves rewritten HTML/bridge while revocation and cross-Origin handoff preserve isolation.
- **A real-loading test for the external entry** (`tests/entry-load.spec.ts`): materializes the profile-local source row and imports it in a plain-Node subprocess (the alpha.5 built CLI's resolution environment), then asserts the exported plugin shape.
- **Component specs** for the panel and host editor (real store engine, stubbed inject/locale) asserting navigation, picker focus handoff, solid editor surface, all allowlisted controls, live style/text preview, per-field reset, exact rollback, structured snapshots, acknowledgement/error/retry/clear states, and per-item removal.
- **Browser e2e** (`pnpm test:e2e`, Playwright Chromium against spawned services): panel open → random-Origin iframe/bridge renders → rich style/text preview → one-field reset → capsule/detail/marker echo → pending acknowledgement → **full send loop: the unchanged stock-composer user message is followed by one separate plugin Context message appended by `agent/pre-step`, the accepted user node consumes the capsule, and all temporary page mutations roll back** → clearing pending state before send produces no context. No fixed sleeps may stand in for acknowledgement boundaries. Keyless-by-design via the **configuration layer** (mirroring the harness scaffold): isolated `$DSH_HOME` with a pre-written welcome-notice acknowledgement (`settings.yaml`, exact version match), the provider key resolved through the product's own chain (inherited environment, repo `.env`, then `~/.dsh/.env`, with the default DSH credentials file copied mode `0600` only when no environment key exists; without any credential a dead-loopback `DEEPSEEK_BASE_URL` makes the probe message fail instantly), the shipped `directory-picker-auto` row replaced by `-browse` (the auto chooser cannot resolve interactions headless), and `llm-deepseek` patched with `retryPolicy.maxRetries: 0` so turns settle without churn. Each scenario connects its own workspace directory so sessions never interfere.
- **`pnpm check`** is the repo gate (typecheck, unit suite, gen-config idempotence, bundle banner-id contract, build); `pnpm check --e2e` adds the browser suite. The harness's own suites (`test:gui`/`test:web`) are not run from this repo — the harness tree is untouched by design. **`pnpm test:e2e` needs the alpha.5 checkout** (`~/Program/deepseek-harness-alpha5`); when the preview moved into the right Sidebar its `openPreviewTab` step (`tests/e2e-scaffold.ts`) was rewritten but could not be run without that checkout, so the next person with it must run the suite and fix the whole entry sequence if the host's guide page differs.
- Store-factory and component tests call `createWebviewStore().create()` directly — the sanctioned zero-machinery path; production code never calls the factory outside `apply`.

## Local development and verification

- **New plugin checkout/worktree bootstrap:**
  1. Run `pnpm install`. Ordinary repository gates use pinned public npm packages anonymously and do not need a Harness checkout.
  2. For development, acceptance, or E2E, ensure the external Harness checkout is on the reviewed alpha.5 tag above (`~/Program/deepseek-harness-alpha5`). Keep it outside this repo and do not patch it for this plugin.
  3. Export `DSH_HARNESS=<absolute harness root>` for those full-composition workflows.
  4. Run `pnpm setup:harness`. It verifies a gitignored build stamp against the checkout's exact commit and installs/cleans/builds when the stamp or required artifacts are stale; it also regenerates the gitignored `cordis.yml` plus `packages/dsh-web-review/entry-name.json` from the stable source package name.
  5. Run `pnpm check` before development. For UI or send-path work, also run `pnpm test:e2e` (or the combined `pnpm check:e2e`). A copied `node_modules`, `cordis.yml`, or `entry-name.json` from another workspace is not valid bootstrap state.
- **New DSH workspace/session bootstrap:** start this plugin with `pnpm dev`, open the reported DSH URL, and connect the source directory the agent is allowed to edit. Any credential-free absolute HTTP(S) URL activates Preview. For the edit-code loop, also start the target application's development server and give the assistant its absolute URL. Sign-in inside Preview persists through the target-Origin cookie jar, but hardcoded absolute APIs and HMR WebSockets still do not work through the isolated transport.
- **Dev loop:** `pnpm dev` runs the alpha.5 built CLI as `dsh web --patch ./cordis.yml --port 3090` AND this package's tsdown watch together — both must run from the same places per the loading model above; neither alone updates the GUI. The launcher-owned `--patch` must precede Web-owned flags such as `--host` and `--port`. If either child exits unexpectedly, the supervisor stops the other and fails the command. Browser refresh applies rebuilt client bundles; node-half changes require restarting the web process (cordis HMR is disabled for web). `pnpm demo` serves the demo page (default port 5173).
- **Manual acceptance reuses the user's profile:** the acceptance `pnpm dev` launch must leave `DSH_HOME` unset unless the user explicitly requests isolation. It therefore reuses the user's normal DSH profile, settings, providers, and product bundle; the generated `cordis.yml` may add only the single absolute-path `dsh-web-review` insert row through `--patch`. If that profile already installs the official bundle, the source overlay would duplicate the `dsh-web-review` row: report the conflict and let the user explicitly choose whether to remove the installed package; never mutate the profile automatically. Never present an isolated test profile as the user's acceptance instance. Isolated temporary `DSH_HOME` directories remain required for automated E2E and are allowed for an explicitly identified concurrent-worktree diagnostic launch.
- **Persistent isolated acceptance (explicit opt-in):** `pnpm dev:acceptance` is the dedicated repeat-testing entry authorized by the user. It keeps its profile, workspace registrations, sessions, and conversation history beneath the gitignored `.artifacts/acceptance/dsh-home`, while running this checkout's development overlay, client watcher, and demo server. The first free web/demo port pair is recorded in `.artifacts/acceptance/ports.json` and reused so saved Preview URLs remain valid across restarts; `DSH_WEB_PORT` / `DEMO_PORT` are temporary overrides. Before web boot, use the resolved Harness's `SessionStore` and JSONL persistence service to create or reuse one closed, provider-free `网页批注验收` fixture turn containing an assistant-authored Demo link; never hand-write the session log or call a model to seed it. This isolated profile is not a substitute for normal-profile acceptance unless the user explicitly asks for it.
- **Persistent acceptance credentials:** preserve the product credential chain without logging secrets. Inherit an existing provider environment variable first; otherwise, on first initialization only, copy `~/.dsh/.credentials.yaml` into the isolated profile with mode `0600`. Never overwrite an existing isolated credential file, print or parse its contents, or commit anything beneath `.artifacts/`.
- **Preview before final verification:** user-facing preview and manual acceptance stay inside DSH; do not add a separate component playground. After implementing a user-visible change, start a real preview service when practical and give the user its clickable DSH URL (plus the demo URL when relevant) so they can inspect the result while automated validation continues. Keep the service alive during acceptance. In a worktree, never assume the default `3090`/`5173` ports are available: probe for unused ports, launch with worktree-specific values such as `DSH_WEB_PORT=<free-port> pnpm dev` and `DEMO_PORT=<free-port> pnpm demo`, regenerate the overlay from that worktree, and do not stop or reuse another checkout's processes. Report the exact URLs, profile posture, and process ownership in the handoff.
- **Credential continuity for isolated previews:** when automated E2E or an explicitly requested diagnostic launch uses an isolated `DSH_HOME`, the isolation must not silently make a user's already-configured provider unavailable. Preserve DSH's credential precedence without exposing secret values: inherit an existing provider environment variable first; otherwise, when the key is stored by DSH, copy the default `~/.dsh/.credentials.yaml` into the temporary `DSH_HOME` with mode `0600`; retain `~/.dsh/.env` only as the product's read-only fallback. Never print, parse into tool output, commit, or embed credentials in Cordis YAML, shell arguments, screenshots, or logs. Remove temporary credential copies when the preview service stops. If no configured credential exists, say so explicitly instead of presenting a keyless screenshot session as a fully verified model loop.
- **Verification ladder:** `pnpm check` (npm-only typecheck + unit + config/bundle contracts + build) → `DSH_HARNESS=<absolute root> pnpm test:e2e` (browser suite; also `pnpm check --e2e`) → manual acceptance. The pre-commit hook runs the npm-only fast subset automatically.
- The running 3080 GUI is served by the product `dsh` binary and is NOT touched by this plugin's dev loop.
- End-to-end acceptance: open a local or remote HTTP(S) page through a random Preview Origin → pick elements → comment → send → model edits workspace source when connected → manual iframe refresh shows the change.

## README media automation

- **README media is regenerated by machine, and the operational runbook lives in the project skill `readme-media`** (`.agents/skills/readme-media/SKILL.md`): `pnpm media` (`--only screenshots|gif`) drives the real DSH Web GUI through `scripts/readme-media.ts` + `scripts/media-hud.ts` and rewrites `docs/assets/web-review-preview.jpg`, `web-review-annotation-editor.jpg`, and `web-review-demo.gif`. Load that skill before touching the assets or the media scripts — it carries the prerequisites, the disposable-run/zero-model-call invariants, and the drive constraints (unitless `font-size`, hex 3-digit-shorthand normalization, 1px-per-px scrub step) that regress if simplified. Never hand-edit the assets.

